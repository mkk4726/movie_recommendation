# Core dependencies
streamlit>=1.29.0
pandas>=2.0.0
numpy>=1.24.0,<2.0.0
scipy>=1.10.0
scikit-learn>=1.3.0
scikit-surprise>=1.1.3
pyyaml>=6.0.1
psutil>=5.9.0

# Web scraping
playwright>=1.40.0
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Firebase
firebase-admin>=6.0.0
google-cloud-firestore>=2.11.0

# Streamlit extensions
streamlit-cookies-manager>=0.2.0

# Environment management
python-dotenv>=1.0.1

# NLP / NER
# Pororo is only installable on Python < 3.9 because it pins torch==1.6.0.
torch>=2.1.0; python_version >= "3.9"
# GLiNER currently requires a Transformers version that still exposes
# legacy utilities; pin an upper bound to avoid ImportError (cached_property).
transformers>=4.38,<4.46; python_version >= "3.9"
tokenizers>=0.15.0; python_version >= "3.9"
gliner>=0.2.10,<0.3.0; python_version >= "3.9"

torch==1.6.0; python_version < "3.9"
torchvision==0.7.0; python_version < "3.9"
pororo==0.4.2; python_version < "3.9"
